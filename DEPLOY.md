# 🚀 Деплой на Railway

## Подготовка

1. Создайте новый репозиторий на GitHub
2. Загрузите код в репозиторий

## Деплой на Railway

### 1. Создание проекта

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий

### 2. Добавление PostgreSQL базы данных

1. В проекте Railway нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway автоматически создаст базу данных

### 3. Настройка переменных окружения

В настройках проекта (Variables) добавьте:

```
TELEGRAM_BOT_TOKEN=8219582907:AAHDnQkkovHsm9d7WkmxKrfXChTOg3fHDBs
ADMIN_TELEGRAM_ID=ваш_telegram_username
TIMEZONE=Asia/Irkutsk
NODE_ENV=production
```

### 4. Подключение к базе данных

Railway автоматически создаст переменные окружения для PostgreSQL:
- `PGHOST`
- `PGPORT`
- `PGDATABASE`
- `PGUSER`
- `PGPASSWORD`

**ВАЖНО:** Нужно использовать Supabase или настроить подключение к PostgreSQL через Railway.

#### Вариант A: Использовать Supabase (рекомендуется)

1. Создайте проект на [supabase.com](https://supabase.com)
2. В настройках проекта найдите "Project Settings" → "API"
3. Скопируйте:
   - `Project URL` → `SUPABASE_URL`
   - `anon public` key → `SUPABASE_ANON_KEY`
4. Добавьте в Railway Variables:
   ```
   SUPABASE_URL=https://ваш-проект.supabase.co
   SUPABASE_ANON_KEY=ваш-ключ
   ```

#### Вариант B: Использовать Railway PostgreSQL напрямую

Нужно будет обновить код для работы с Railway PostgreSQL напрямую (сейчас используется Supabase клиент).

### 5. Инициализация базы данных

1. В Railway откройте созданную PostgreSQL базу данных
2. Перейдите в "Data" → "Query"
3. Скопируйте содержимое `migrations.sql`
4. Выполните SQL запросы для создания таблиц

### 6. Деплой

1. Railway автоматически начнет деплой при пуше в репозиторий
2. Или нажмите "Deploy" вручную
3. Проверьте логи в разделе "Deployments"

### 7. Проверка работы

1. Откройте Telegram бота: `t.me/isib_manager_bot`
2. Отправьте `/start`
3. Проверьте, что бот отвечает

## Обновление

Просто сделайте `git push` в ваш репозиторий - Railway автоматически перезапустит бота с новым кодом.

## Мониторинг

- **Logs:** Railway → ваш проект → "Deployments" → выберите деплой → "View Logs"
- **Metrics:** Railway → ваш проект → "Metrics"

## Troubleshooting

### Бот не отвечает
- Проверьте логи в Railway
- Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что база данных подключена

### Ошибки подключения к БД
- Проверьте переменные окружения `SUPABASE_URL` и `SUPABASE_ANON_KEY`
- Убедитесь, что таблицы созданы (выполнен `migrations.sql`)

### Бот падает при старте
- Проверьте логи на наличие ошибок
- Убедитесь, что все переменные окружения установлены
- Проверьте синтаксис кода

